services:
  browser-glue:
    build:
      context: .
      dockerfile: projects/browser_automation/Dockerfile
    env_file:
      - .env
    environment:
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - AWS_SESSION_TOKEN
    volumes:
      - ./.data:/app/.data
      - ./.out:/app/.out
