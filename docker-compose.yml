services:
  browser-glue:
    build:
      context: .
      dockerfile: projects/browser_automation/Dockerfile
    env_file:
      - .env
    stdin_open: false
    entrypoint: ["sh"]
    tty: false
    environment:
      - PYTHONUNBUFFERED=1
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - AWS_SESSION_TOKEN
    volumes:
      - ./.data:/app/.data
      - ./.out:/app/.out
